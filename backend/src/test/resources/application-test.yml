spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:zoo_reserve_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=USER;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      encoding: UTF-8
